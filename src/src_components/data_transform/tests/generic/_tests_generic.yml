version: 2
data_tests:
  - name: validate_month
    description: Ensure that for every record, one of two timestamp columns falls within `month_idx`
    arguments:
      - name: model
        type: string
        description: Model name
      - name: started_at_col
        type: string
        description: 
      - name: ended_at_col
        type: string
        description:
      - name: month_idx
        type: integer
        description: Index of month to check against. For example if you want all records to be in January, pass "1". 
  - name: validate_2020_data
    description: "{{ doc('test_validate_YYYY_data') }}"
    arguments:
      - name: model
        type: string
        description: Model name
  - name: validate_2021_data
    description: "{{ doc('test_validate_YYYY_data') }}"
    arguments:
      - name: model
        type: string
        description: Model name
  - name: validate_2022_data
    description: "{{ doc('test_validate_YYYY_data') }}"
    arguments:
      - name: model
        type: string
        description: Model name
  - name: validate_2023_data
    description: "{{ doc('test_validate_YYYY_data') }}"
    arguments:
      - name: model
        type: string
        description: Model name
  - name: validate_2024_data
    description: "{{ doc('test_validate_YYYY_data_V2') }}"
    arguments:
      - name: model
        type: string
        description: Model name
  - name: validate_2025_data
    description: "{{ doc('test_validate_YYYY_data_V2') }}"
    arguments:
      - name: model
        type: string
        description: Model name